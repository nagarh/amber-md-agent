# AmberMD Agent — Dependencies
# Conda environment: amber_development  (/home/hn533621/.conda/envs/amber_development)
#
# Create + install:
#   conda create -n amber_development python=3.11 -y
#   conda install -n amber_development -c conda-forge \
#     numpy scipy matplotlib rdkit parmed mdanalysis propka biopython pdfminer.six -y
#
# Always use: /home/hn533621/.conda/envs/amber_development/bin/python

# Installed (pinned versions in env):
numpy>=2.0          # array math, TI analysis
scipy>=1.17         # trapezoid integration, stats
matplotlib>=3.10    # plots
rdkit>=2026.03      # ligand preparation (Branch C pipeline)
parmed>=4.3         # topology manipulation, tiMerge
MDAnalysis>=2.10    # cap_protein.py (add_caps.py)
propka>=3.5         # propka3 — HIS/ASP/GLU pKa prediction
biopython           # structure utilities
pdfminer.six        # Amber manual RAG PDF ingestion

# NOT here — come from AmberTools SLURM module:
# tleap, cpptraj, antechamber, pmemd, sander, parmchk2, pdb4amber
# module load gnu12/12.2.0 && module load amber/24
